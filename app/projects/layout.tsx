export const metadata = {
  title: "Projects | Jialin Yang",
  description:
    "Selected production engineering work: live applications and archived projects.",
};

export default function ProjectsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
