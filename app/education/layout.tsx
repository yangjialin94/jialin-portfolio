export const metadata = {
  title: "Education | Jialin Yang",
  description:
    "Academic background in computer science and artificial intelligence.",
};

export default function EducationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
