// Add a global type declaration to extend the Window interface.
// This ensures TypeScript recognizes window.grecaptcha.

declare global {
  interface Window {
    grecaptcha: {
      ready: (callback: () => void) => void;
      execute: (
        siteKey: string,
        options: { action: string }
      ) => Promise<string>;
    };
  }
}

export {};
